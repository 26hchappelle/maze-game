{
  "name": "maze-escape-game",
  "version": "1.0.0",
  "description": "A pixelated maze escape game",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "claude": "^0.1.1"
  }
}
